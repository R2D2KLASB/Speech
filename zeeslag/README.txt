zeeslag node needs to be run as root, use `sudo -E su` to login as root while preserving ros2 environment to allow communication

export ROS_MASTER_URI=http://localhost:11311
. /home/ubuntu/ros2_foxy/ros2-linux/setup.bash
